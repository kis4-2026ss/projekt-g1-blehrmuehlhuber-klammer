Ideas: meta llm gets propmt and decides which llm is suited best for it and forwards the prompt. Decision can be based on several factors e.g. cost, quality.
      meta llm gets prompt and generates several prompts out of that parent prompt and delegates the different child prompts to the best suited llms and builds 1 request answer from those outputs.
      meta llm checks the respond from the child llm and if necesarry adds contraints/rules to the prompt and delegates it again.
      meta llm sends the same prompt to several child llms and compares the responds. If they don't allign it is to be defiend what the meta llm does. In any case it generates 1 meta respons.
